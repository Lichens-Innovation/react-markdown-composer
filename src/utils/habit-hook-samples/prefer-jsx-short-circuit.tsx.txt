/** habit-hooks-disable non-essential-comment
 * This is a "Demo" example that should trigger the prefer-jsx-short-circuit rule.
 */

interface DemoCountProps {
  count: number;
}

export const DemoCount = ({ count }: DemoCountProps): JSX.Element => {
  return <div>{count && <span>{count} items</span>}</div>;
};
