/** habit-hooks-disable non-essential-comment
 * This is a "Demo" example that should trigger the require-fallback-on-deep-chain rule.
 */

interface Nested {
  a?: { b?: { c?: string } };
}

export const readDeepValue = (obj: Nested): string | undefined => {
  return obj?.a?.b?.c;
};
