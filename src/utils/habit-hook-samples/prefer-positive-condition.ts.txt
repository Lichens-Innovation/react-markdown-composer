/** habit-hooks-disable non-essential-comment
 * This is a "Demo" example that should trigger the prefer-positive-condition rule.
 */

export const describeReadiness = (isReady: boolean): string => {
  return !isReady ? "waiting" : "ready";
};
