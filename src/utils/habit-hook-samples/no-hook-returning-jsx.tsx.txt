/** habit-hooks-disable non-essential-comment
 * This is a "Demo" example that should trigger the no-hook-returning-jsx rule.
 */

import { useId } from "react";

export const useGreeting = (name: string): JSX.Element => {
  const id = useId();
  return <span id={id}>Hello, {name}</span>;
};
